# Set the path to your network weights before starting the bridge.
# Example:
# --weights=/path/to/weights.pb.gz
--backend=cudnn-auto
--preload
--threads=2
--minibatch-size=16
--max-concurrent-searchers=1
--multipv=1
--show-wdl=false
--nncache=400000
--move-overhead=0
